# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'myFirstFireBase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for myFirstFireBase
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'

  target 'myFirstFireBaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'myFirstFireBaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
